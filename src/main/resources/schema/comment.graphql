type Comment{
    id : ID!
    content: String!
    member: Member!
}
input CreateCommentInput{
    postId : ID!
    content: String!
}

extend type Mutation {
    createCommentByPostId(input:CreateCommentInput) : Comment
}
extend type Query {
    deleteCommentByPostId(id:ID) : Boolean
}